我想要的虚拟机环境:
    1. 内核是我的Litmus编译出来的
    2. 有多组任务集和实时程序
    3. 可以计算实时程序的实时性能

如何让虚拟机的内核是我编译出来的内核呢？目前有三种方法
    第一种是在本地编译，然后制作类似镜像的东西，然后用镜像创建虚拟机
    第二种也是本地编译，然后将编译出来的内核上传到github，然后在虚拟机中更换内核
    第三种是在虚拟机上编译，然后更换内核
第二种相对更简单，第一种更稳妥，第三种则没有明显的优点。

听说Litmus官网上有现成的镜像，但是终究还是要自己编写编译，官网上的镜像只能做一个参考。

任务集和实时程序可以通过在本地编写生成，并上传到github，然后在虚拟中拉取来实现。

暂时不知道如何计算实时性能，以及如何生成图表之类的东西。

目前先这样，先看代码，理解gFPca的实现并制作PPT。
